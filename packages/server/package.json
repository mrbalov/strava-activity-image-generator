{
  "name": "@torq/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/server.ts --outdir dist --target node --format esm --minify",
    "build:watch": "bun build src/server.ts --outdir dist --target node --format esm --watch",
    "build:netlify": "bun build netlify/functions/*.ts --outdir netlify/functions --target node --format esm",
    "dev": "bun --watch src/server.ts",
    "start": "node dist/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^2.8.2",
    "@torq/check-forbidden-content": "workspace:*",
    "@torqlab/generate-activity-image": "^1.0.0",
    "@torqlab/get-activity-image-generation-prompt": "^1.0.1",
    "@torqlab/get-strava-activity-signals": "^1.0.1",
    "@torqlab/strava-api": "^1.1.1"
  }
}
